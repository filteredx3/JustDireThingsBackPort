# Release Artifacts

Place distributable build artifacts in this folder.

For the 1.12.2 backport build, run from repo root:

```bash
./scripts/backport/build_release_jar.sh
```

Expected output:
- A built `.jar` copied from `backport-1.12.2/build/libs/` into this `release/` folder.
